DATABASE_URL=postgresql://pc:pc@localhost:5432/popular_consensus
ARTIFACT_DIR=./data/artifacts
PC_DEV_MODE=true
NEXT_PUBLIC_API_URL=http://localhost:4000
PC_AA_RPC_URL=http://127.0.0.1:8545
# Optional: use a chain-native or external P-256 verifier instead of the local deployment fallback.
PC_AA_P256_VERIFIER=
# Optional: route signed UserOperations through a local ERC-4337 bundler RPC instead of direct EntryPoint handleOps.
PC_AA_BUNDLER_URL=

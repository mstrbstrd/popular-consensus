# MVP Readiness Check

Audit timestamp: 2026-05-09T14:17:17Z

Objective: finish the Popular Consensus MVP using `docs/decentralized-protocol-roadmap.md` as the working tracker toward a shared, community-governed public opinion protocol.

Decision: Not complete. The repo is locally ready for the final public testnet process, but the final roadmap gate still requires real independent operators, published evidence, and a maintainer launch summary.

This audit is a checkpoint, not the source of truth. Re-run the commands in the checklist before changing the final roadmap checkbox.

## Success Criteria

1. `docs/decentralized-protocol-roadmap.md` has no open or in-progress checklist items.
2. `pnpm mvp:audit` reports `Ready`.
3. Public testnet evidence exists in `docs/public-testnet-attestations` with at least:
   - 1 deployer evidence file.
   - 2 API/indexer evidence files.
   - 3 replay-checker evidence files with matching transaction and event stream hashes.
   - 2 community-guide evidence files with governance and safety drills complete.
4. `docs/public-testnet-launch-summary.md` exists, includes all operator ids and evidence-file hashes, records unresolved issues, has `Independence status: reviewed`, and says `Decision: GO`.
5. `pnpm testnet:verify-attestations` reports `Ready`.
6. Public-testnet coordination artifacts remain auditable with `pnpm testnet:audit-roster`.
7. Local verification remains green: `pnpm test`, `pnpm typecheck`, and `pnpm test:api:db`.

## Prompt-To-Artifact Checklist

| Requirement | Evidence inspected | Verification | Status |
| --- | --- | --- | --- |
| Use `docs/decentralized-protocol-roadmap.md` as the working tracker. | `docs/decentralized-protocol-roadmap.md`; `pnpm mvp:audit -- --json`; `pnpm mvp:audit:strict`. | Audit report reads the roadmap and counted 50 ranked roadmap items; strict mode exits non-zero because the final public-testnet gate is still open. The top-level audit also reports public-testnet outreach, roster, evidence status, and workspace warnings such as the missing Git remote. | Pass, final gate pending |
| Build toward a shared protocol, not only a local app. | Roadmap sections 1-9; `docs/appchain-module-boundary.md`; `packages/contracts/src/PopularConsensus.sol`; public API protocol transaction/replay paths. | Roadmap sections 1-8 are complete; Section 9 has 5 of 6 items complete. | Pass except final public testnet |
| Community-governed opinion polling MVP. | Roadmap governance, voting-pass, counting, appchain, and public API items; `apps/api/src/server.ts`; shared schemas. | Local API and contract tests cover community rules, next-step rules, flags, voting passes, polling, counting, archives, and replay contracts. | Pass locally |
| Checkable public records. | Roadmap Section 1; `packages/artifacts/src/index.ts`; artifact tests. | `pnpm test` runs artifact tests; latest run passed. | Pass |
| Public API as indexer/client layer. | Roadmap Section 2; `apps/api/src/server.ts`; `packages/shared/src/index.ts`. | API response schemas and root test passed; DB integration passed in `pnpm test:api:db`. | Pass |
| On-chain/public audit commitments. | Roadmap Section 3; `packages/contracts/test/PopularConsensus.t.sol`; local devnet runbook. | `pnpm test` ran Foundry contract tests: 11 passed. | Pass |
| Community fork and alternate frontend support. | Roadmap Section 4; `docs/community-fork-and-exit.md`; community export/import replay paths. | Covered by roadmap completion and API integration tests. | Pass |
| Federated social layer. | Roadmap Section 5; public discussion/profile/discovery/reputation API surfaces. | Covered by roadmap completion and API integration tests. | Pass |
| Community rules and legitimacy system. | Roadmap Section 6; next-step rules, challenge, juror, community-guide, and governance parameter APIs. | Covered by roadmap completion, contract tests, and API DB test. | Pass |
| Portable voting-pass ecosystem. | Roadmap Section 7; voting-pass schemas, issuer list, trust policies, nullifier proof paths. | Covered by shared/privacy/API tests and roadmap completion. | Pass |
| Private vote counting. | Roadmap Section 8; counting committee/key/share/proof/failure flows. | Covered by roadmap completion, contract tests, and API DB test. | Pass |
| Full protocol/appchain as source of truth. | Roadmap Section 9; canonical boundary, contracts, protocol transaction feed, replay endpoint, upgrade safety endpoint. | 5 of 6 Section 9 items complete. | Partial |
| Run public testnet with independent operators. | `docs/public-testnet-operator-runbook.md`; `docs/public-testnet-attestations`; `docs/public-testnet-launch-summary.md`; `pnpm mvp:audit`. | `pnpm mvp:audit` reports 0 evidence files and missing launch summary. | Missing |
| Public testnet operator evidence intake is ready. | `GET /public/protocol/testnet-readiness`; `docs/public-testnet-operator-runbook.md`; `docs/public-testnet-maintainer-checklist.md`; `scripts/collect-public-testnet-attestation.ts`; `scripts/verify-public-testnet-attestations.ts`; `scripts/write-public-testnet-launch-summary.ts`. | `pnpm test:public-testnet-tools` builds a complete temporary evidence set, writes a reviewed `GO` summary, verifies `Ready`, and confirms duplicate contacts are blocked. The operator runbook links maintainers to the checklist for the public issue, outreach, roster, evidence files, launch-summary, and final-audit sequence. | Pass locally |
| Public testnet operator recruitment packet exists. | `README.md`; `docs/public-testnet-external-input-request.md`; `docs/public-testnet-maintainer-checklist.md`; `.github/PULL_REQUEST_TEMPLATE/public-testnet-attestation.md`; `docs/public-testnet-operator-invitation.md`; `docs/public-testnet-operator-recruitment-targets.md`; `docs/public-testnet-operator-outreach-queue.md`; `docs/public-testnet-operator-send-packets.md`; `docs/public-testnet-operator-outreach-messages.md`; `docs/public-testnet-operator-outreach-log.md`; `scripts/record-public-testnet-outreach.ts`; `scripts/audit-public-testnet-outreach.ts`; `pnpm testnet:audit-outreach:strict`; `scripts/audit-mvp-readiness.ts`; `docs/public-testnet-operator-assignment-intake.md`; `docs/public-testnet-operator-issue-drafts.md`; `docs/public-testnet-operator-issue-bodies`; roster and runbook links. | Maintainers have a launch-path checklist, an external input request sheet, a public-testnet PR checklist, a sourced candidate-pool guide, a first-contact slot queue, per-slot send packets, safe outreach-log record/audit helpers, outreach message variants, prospect outreach log, invitation template, assignment intake template, generated issue drafts, a manual issue-creation table, per-slot roster tracking commands, GitHub CLI body files, role-specific evidence command shapes, and acceptance criteria. The root README points to issue URL intake validation with `pnpm testnet:record-issue-urls -- --dry-run`, recording with `pnpm testnet:record-issue-urls`, explicit-repo issue command generation, and `pnpm testnet:operator-issue-drafts -- --json` for authenticated GitHub connector issue creation after approval. `pnpm test:public-testnet-tools` covers the outreach helpers against temporary logs, including roster tracking-issue coverage counts, roster-derived outreach tracking issue recording with `--tracking-issue-from-roster`, guardrails for missing roster issue URLs, audit blocking when outreach tracking issues drift from roster tracking issues, and an MVP-audit fixture that verifies the next actions link the maintainer checklist, the authenticated GitHub connector fallback, manual issue-creation fallback, issue URL dry-run validation, roster issue-recording command, and roster-derived outreach recording. `pnpm lint:placeholders` now scans README, `.github`, docs, e2e, infra, packages, apps, and scripts. `pnpm testnet:audit-outreach:strict` exits non-zero until every slot has contacted-or-later outreach, and `pnpm mvp:audit` surfaces outreach status plus roster tracking issue coverage, assigned status, and reviewed status. | Pass locally |
| Public operator issue creation is ready to hand off. | `docs/public-testnet-maintainer-checklist.md`; `docs/public-testnet-operator-issue-drafts.md`; `docs/public-testnet-operator-issue-bodies/README.md`; `docs/public-testnet-operator-issue-url-intake.md`; `scripts/record-public-testnet-issue-urls.ts`; `git remote -v`; `gh --version`; `pnpm testnet:operator-issue-drafts -- --github-repo <owner/repo>`; `pnpm testnet:operator-issue-drafts -- --json`; `pnpm testnet:operator-issue-drafts:check`; `pnpm testnet:record-issue-urls`; `pnpm testnet:update-roster-slot`. | Local issue drafts and one body file per slot exist for all eight operator slots, and `pnpm testnet:operator-issue-drafts:check` verifies they are current, including missing, stale, and extra stale body files. Regeneration prunes extra stale markdown body files before rewriting the generated directory. The issue-body README includes a manual issue-creation table with slot, title, labels, and body file for the no-`gh` path, plus a connector payload section that points to `pnpm testnet:operator-issue-drafts -- --json`; the JSON form includes each draft's title, body, and labels for authenticated GitHub connector issue creation after explicit approval. The maintainer checklist now gives the exact connector sequence: generate JSON, create one issue per `drafts[]` entry with approved `repository_full_name`, capture returned issue URLs in the intake sheet, dry-run, then record. The issue URL intake sheet gives maintainers one place to collect the eight public issue URLs, `pnpm testnet:record-issue-urls -- --dry-run` reports planned updates without mutating the roster, and `pnpm testnet:record-issue-urls` records non-`open` intake rows on the roster in one pass while validating slot/role alignment. The issue generator now drafts only unassigned slots whose `Tracking Issue` cell is still `open`, explicitly reports when no new issue drafts are needed, and `pnpm test:public-testnet-tools` covers that tracked-but-unassigned slots do not receive duplicate issue drafts, connector JSON labels, dry-run issue URL recording leaves the roster unchanged, and the all-tracked state has clear no-op output. The maintainer checklist, generated issue drafts, issue-body README, and roster docs record that maintainers should record created issue URLs before regenerating. This workspace currently has no Git remote and no `gh` command available, so public issue creation requires a repo remote, GitHub default repo, explicit `--github-repo <owner/repo>` target, approved connector `repository_full_name`, maintainer approval, and a GitHub connector, GitHub CLI, or manual issue-creation path. | Blocked externally |
| Public testnet role commands are operator-specific. | `docs/public-testnet-role-command-reference.md`; runbook and invitation links. | Each required role has a concrete `pnpm testnet:collect-attestation` evidence command shape and maintainer verification commands. | Pass locally |
| Public testnet rehearsal is bounded. | `docs/public-testnet-rehearsal-plan.md`; runbook link. | Maintainers have a dry-run process that uses temporary evidence paths and explicitly forbids using rehearsal evidence to close the final gate. | Pass locally |
| Public testnet roster is trackable. | `docs/public-testnet-operator-roster.md`; `pnpm testnet:update-roster-slot`; `pnpm testnet:audit-roster`; `pnpm testnet:audit-roster:strict`; `pnpm mvp:audit`. | Roster audit reports tracking issue coverage, required slots, and assigned/reviewed operators; current real roster tracking issue coverage is 0/8 and assigned/reviewed coverage is still 0 for every role. Assignment updates can be applied without hand-editing the table; direct `pnpm testnet:audit-roster:strict` exits non-zero until every required slot is reviewed with evidence; `pnpm mvp:audit` includes roster status. | Pass locally, pending operators |
| Public testnet verifier final gate. | `pnpm testnet:verify-attestations`; default evidence directory and launch summary. | Direct `pnpm testnet:verify-attestations` exits non-zero because it reports 0 evidence files, missing deployer 0/1, API/indexer 0/2, replay-checker 0/3, community-guide 0/2, and missing launch summary. | Missing |
| Maintainer independence review. | `docs/public-testnet-launch-summary.template.md`; summary writer `--independence-reviewed`; verifier launch-summary inspection. | Tooling requires the reviewed status for `GO`, but no real reviewed launch summary exists. | Missing |
| Local test suite. | Root package scripts. | Latest `pnpm test` run passed on 2026-05-09T14:16Z, including Foundry contracts, shared/artifact/privacy tests, API smoke tests, the public-testnet tool smoke test, and generated operator issue handoff freshness check. | Pass |
| Local typecheck. | Root package scripts. | Latest `pnpm typecheck` run passed on 2026-05-09T14:16Z across workspace packages and script typechecking. | Pass |
| DB-backed API integration. | `pnpm test:api:db`. | Latest run passed with 26 API DB integration tests. | Pass |

## Current Missing Evidence

- No real `public-testnet-operator-attestation-v0` evidence files are present in `docs/public-testnet-attestations`.
- Candidate routes are identified for all 8 required slots in `docs/public-testnet-operator-outreach-log.md`, but no real outreach attempts are recorded; `pnpm mvp:audit` reports identified slots 8/8 and contacted slots 0/8.
- No real operators are assigned in `docs/public-testnet-operator-roster.md`.
- No `docs/public-testnet-launch-summary.md` exists.
- No maintainer has recorded `Independence status: reviewed` for real operators.
- No Git remote is configured and `gh` is unavailable in this workspace, so public operator tracking issues cannot be created from here until a repo target or approved connector `repository_full_name`, maintainer approval, and a GitHub connector, GitHub CLI, or manual issue-creation path are provided.
- Latest issue URL intake dry run: `pnpm testnet:record-issue-urls -- --dry-run` would record 0 tracking issue URLs because no non-`open` issue URLs are present in `docs/public-testnet-operator-issue-url-intake.md`.
- Remaining `needs-*`, `<owner/repo>`, and `<issue-url-or-number>` placeholders are confined to operator issue/send templates and are expected until a maintainer provides public repo, tracking issue, and operator values.
- `pnpm testnet:verify-attestations` cannot report `Ready` until the above evidence exists.
- `pnpm mvp:audit:strict` exits non-zero until the final roadmap item and public-testnet evidence gates are complete.
- Direct strict coordination checks currently fail as expected: `pnpm testnet:audit-outreach:strict` reports contacted slots 0/8, and `pnpm testnet:audit-roster:strict` reports tracking issues 0/8 plus no assigned or reviewed operators.
- The final roadmap checkbox at `docs/decentralized-protocol-roadmap.md` remains open.

## Next Completion Steps

1. Follow `docs/public-testnet-maintainer-checklist.md`, starting with a Git remote, GitHub default repo, or explicit `--github-repo <owner/repo>` target plus maintainer approval to create public operator tracking issues through an authenticated GitHub connector, the GitHub CLI, or manual issue creation, then collect the issue URLs in `docs/public-testnet-operator-issue-url-intake.md`, validate them with `pnpm testnet:record-issue-urls -- --dry-run`, and record them with `pnpm testnet:record-issue-urls`.
2. Recruit independent operators for the required deployer, API/indexer, replay-checker, and community-guide roles using `docs/public-testnet-operator-outreach-queue.md`, `docs/public-testnet-operator-send-packets.md`, `docs/public-testnet-operator-recruitment-targets.md`, `docs/public-testnet-operator-invitation.md`, and `docs/public-testnet-operator-roster.md`.
3. Capture real assignment fields with `docs/public-testnet-operator-assignment-intake.md`.
4. Send first-wave outreach using `docs/public-testnet-operator-send-packets.md`, then record contacted rows with `pnpm testnet:record-outreach -- --tracking-issue-from-roster`.
5. Check prospect-log consistency with `pnpm testnet:audit-outreach`.
6. Record invited or accepted operators with `pnpm testnet:update-roster-slot`, then run `pnpm testnet:audit-roster`.
7. Optionally rehearse the workflow with `docs/public-testnet-rehearsal-plan.md` without copying rehearsal evidence into the real evidence paths.
8. Have each operator follow `docs/public-testnet-operator-runbook.md` and `docs/public-testnet-role-command-reference.md`.
9. Commit each generated evidence JSON to `docs/public-testnet-attestations`.
10. Run `pnpm testnet:write-launch-summary -- --decision GO --independence-reviewed --testnet-window <window>` only after maintainer independence review passes.
11. Run `pnpm testnet:verify-attestations` and require `Ready`.
12. Re-run `pnpm mvp:audit`; only mark the roadmap and active goal complete if it reports `Ready`.

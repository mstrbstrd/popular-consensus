{
  "name": "@pc/contracts",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "forge build --offline",
    "test": "forge test --offline --disable-labels",
    "deploy:local": "forge build --offline && node scripts/deploy-local.mjs"
  },
  "dependencies": {
    "viem": "^2.39.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
